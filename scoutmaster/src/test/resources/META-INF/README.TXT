This META-INF is just for persistence.xml.

The build process will copy this directory to the WEB-INF/classes
directory where eclipselink expects to find it.

NOTE: don't confuse this directory with the webapp/META-INF directory which serves a different purpose.